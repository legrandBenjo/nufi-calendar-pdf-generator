import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.legrandbenjo.calendarpdfgenerator',
  appName: 'Calendar PDF Generator',
  webDir: 'build'
};

export default config;
